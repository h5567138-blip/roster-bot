services:
  - name: roster-bot
    type: web
    github:
      owner: h5567138-blip
      repo: roster-bot
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    plan: free