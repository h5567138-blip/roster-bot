services:
  - type: bgworker
    name: roster-bot
    runtime: python
    github:
      owner: h5567138-blip
      repo: roster-bot
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    plan: free